apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: springboot
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/Lakshmansai1999/springboot-helm.git
    targetRevision: master
    path: springboot

  destination:
    server: https://kubernetes.default.svc
    namespace: default

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
